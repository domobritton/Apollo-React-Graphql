type Resolution {
    _id: String!
    name: String!
    goals: [Goal]
}

type Query {
    hi: String
    resolutions: [Resolution]
}

type Mutation {
    createResolution(name: String!): Resolution 
}
